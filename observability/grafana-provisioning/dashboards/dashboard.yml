# Grafana Dashboard Provisioning Configuration

apiVersion: 1

providers:
  - name: 'openwebui-analytics'
    orgId: 1
    folder: 'OpenWebUI'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards